import SwiftUI

struct ConfigurationView: View {
    var body: some View {
        VStack {
            Text("Configuración")
                .font(.largeTitle)
            // Agrega más contenido aquí
        }
        .padding()
    }
} 
